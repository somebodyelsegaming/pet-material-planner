.DS_Store
__pycache__/
*.pyc

# Working notes from the build session. Kept local on purpose: they are
# written around a real account's material counts and pet levels, and read
# as a record of a conversation rather than project documentation.
# The durable design rationale belongs in a committed doc of its own.
SESSION-*

# Styled render of ISA.md, generated by the ISA tooling. ISA.md is the source
# of truth and GitHub renders it natively, so a committed copy only goes stale.
# Regenerate locally when a standalone styled copy is wanted.
ISA.html
